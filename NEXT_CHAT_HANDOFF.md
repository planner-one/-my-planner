# 다음 채팅 인수인계

_작성일: 2026-06-28_

이 파일은 새 Codex 채팅에서 바로 이어 작업하기 위한 현재 상태 메모입니다.
새 채팅에서는 먼저 이 파일과 `AGENTS.md`, `PROGRESS.md`, `REQUIREMENTS.md`, `SCENARIOS.md`를 읽고 시작하면 됩니다.

## 한 줄 요약

플래너는 Firebase 기반 React/Vite 앱이며, SC-07 위젯 검토는 완료된 상태입니다. 다음 큰 흐름은 SC-08 페이지 11개를 하나씩 검토하고, 이후 SC-09 테마 최종 정리, SC-10 모바일 반응형 정식 검토로 넘어가는 것입니다.

## 작업 위치와 원격

- 로컬 경로: `/Users/minsujeong/planner`
- GitHub remote: `https://github.com/planner-one/-my-planner.git`
- 브랜치: `main`
- 배포: Firebase Hosting
- 배포 URL: `https://my-planner-487bd.web.app`
- 현재 `main`은 `origin/main`과 동일합니다.
- 현재 로컬 변경: `.claude/settings.local.json`만 수정되어 있습니다.
- `.claude/settings.local.json`은 Codex/Claude 로컬 설정이므로 앱 작업 커밋에 포함하지 마세요.

## 최근 커밋 상태

현재 HEAD:

```text
b39cf0e fix: 잘못 추가한 GitHub Pages 배포 설정 되돌림
```

최근 작업 흐름:

```text
b39cf0e fix: 잘못 추가한 GitHub Pages 배포 설정 되돌림
d2cb767 ci: GitHub Pages 자동 배포 워크플로우 추가
12fe2f8 feat: 저널 위젯 제목 숨기고 이미지가 카드 전체를 채우도록 변경
33d8060 fix: 이미지가 있으면 아래 중복 텍스트 제거, 이미지가 카드 전체 채움
a3751a3 feat: 저널 위젯 타이틀 라벨 제거, 슬라이드 버튼을 이미지 위에 오버레이
35fb3b2 feat: 저널 위젯 상태 텍스트 제거, 새로고침을 제목줄로 이동, 이미지 표시 지원 추가
92a9b12 feat: 저널 위젯 웹훅 URL 고정, 설정 UI 제거
f1d5cbe feat: 저널 위젯을 캐러셀(슬라이드) 형태로 변경
1c7e4f3 fix: 저널 위젯이 title/link만 있는 RSS 형태 응답도 정상 표시하도록 수정
3ac118b feat: 저널 알림 위젯 추가 (n8n 웹훅 연동)
```

## 사용자 작업 방식

- 사용자는 한 번에 여러 기능을 임의 진행하는 것을 원하지 않습니다.
- 기능 하나 검토 → 문제 의논 → 수정 → 사용자 확인 → 커밋/푸시 순서로 진행합니다.
- 작업 후 사용자가 확인하거나 명시하면 커밋/푸시합니다.
- 배포 반영이 필요한 경우 Firebase Hosting으로 배포합니다.
- GitHub Pages는 사용하지 않는 방향으로 되돌렸습니다. 현재 기준 배포는 Firebase Hosting입니다.

## 실행과 검증 명령

개발 서버:

```bash
npm run dev
```

빌드:

```bash
npm run build
```

타입 체크:

```bash
npx tsc --noEmit
```

Firebase 배포:

```bash
firebase deploy --only hosting
```

주의:

- 사용자가 예전에 `5173` 포트가 사용 중이라고 말한 적이 있습니다.
- dev 서버를 켤 때 포트 충돌이 있으면 Vite가 다른 포트를 쓸 수 있습니다.
- 배포 후 사용자가 실제 배포 URL에서 확인하는 흐름이 자주 있었습니다.

## 현재 앱 구조

주요 진입:

- `src/App.tsx`: 로그인 여부, 데이터 로딩, 현재 페이지 렌더링
- `src/components/PageShell.tsx`: 사이드바, 모바일 하단 탭, 테마, 로그아웃, 홈 복귀 버튼
- `src/store/AppContext.tsx`: Firestore 사용자 데이터 전체 상태와 저장 로직
- `src/types/index.ts`: 앱 데이터 타입
- `src/widgets/index.ts`: 위젯 등록 목록

데이터 저장:

- 로그인 사용자 데이터는 `AppContext`에서 관리합니다.
- 일반 상태 변경은 1초 debounce 저장입니다.
- 레이아웃 저장, 로그아웃 전 저장 등은 즉시 저장 함수가 있습니다.
- 사용자별 화면 비율 `uiScale`은 Firestore에 저장됩니다.

라우팅:

- 별도 URL 라우터보다는 `RouterContext`의 `page` 값으로 페이지를 바꿉니다.
- 사이드바/하단 탭에서 페이지 전환합니다.

## 현재 페이지 목록

`src/App.tsx` 기준 현재 페이지:

1. `dashboard`: 대시보드
2. `habits`: 습관 트래커
3. `tasks`: 작업 관리
4. `todos`: 오늘 할 일
5. `goals`: 목표 관리
6. `projects`: 프로젝트
7. `weekly`: 주간 플래너
8. `daily`: 일일 플래너
9. `notes`: 노트
10. `journal`: 저널
11. `profile`: 프로필
12. `inquiries`: 문의
13. `print`: 플래너 출력
14. `career`: 신청 관리

문서상 SC-08은 "페이지 10개"로 남은 곳이 있으나, 실제 구현과 시나리오 기준으로는 문의/신청 관리까지 포함되어 페이지 검토 대상이 더 많습니다. 다음 작업에서는 이 불일치도 정리해야 합니다.

## 위젯 상태

`PROGRESS.md` 기준 SC-07 위젯 검토는 완료입니다.

통과:

- 현재 시각
- 날씨
- 오늘 할 일
- 일일 루틴
- 빠른 메모
- 집중 타이머
- 카운터
- 생산성 추이
- 목표
- 예정된 작업
- 하루 마무리
- 메뉴

통합/삭제:

- 최우선 목표는 목표 위젯에 통합
- 빠른 추가 위젯은 오늘 할 일 위젯과 중복되어 삭제

추가로 최근 저널 알림 위젯이 추가되어 있습니다.

## 최근 핵심 기능 상태

### 신청 관리와 캘린더

- 신청 관리 페이지: `src/pages/CareerEvents.tsx`
- 캘린더 위젯: `src/widgets/CalendarWidget.tsx`
- 타입: `CareerEvent`

현재 반영된 사항:

- 신청/지원 일정을 별도 페이지와 캘린더에서 생성할 수 있습니다.
- 구분: 채용설명회, 면접, 직무캠프, 교육/프로그램, 행사/세미나, 공모전, 지원사업, 기업 지원, 기타
- 상태: 관심, 신청 예정, 신청 완료, 결과 대기, 선정/확정, 완료, 탈락, 취소
- 신규 작성 상태 선택에서는 탈락/취소를 제외했습니다.
- 장소와 주소는 `장소 / 주소` 한 항목으로 통합했습니다.
- 온라인이면 장소 입력은 숨깁니다.
- 관련 링크는 온라인/오프라인/혼합과 무관하게 항상 입력 가능합니다.
- 분류별로 신청 마감일, 결과 발표일, 운영 시작/종료일 입력이 다르게 표시됩니다.
- 운영 기간은 평일 기준으로 계산하고 캘린더에도 운영일로 표시합니다.
- 캘린더는 신청 마감, 결과 발표, 운영, 대표 일정일을 구분 표시합니다.

주의할 점:

- `CareerEvents.tsx`의 `lastRelevantDate`는 현재 `item.date`만 기준으로 지난 일정을 나눕니다. 운영 종료일이나 결과 발표일을 기준에 넣을지 추후 검토 여지가 있습니다.

### 생산성 추이

- 계산 로직: `src/utils/productivity.ts`
- 위젯: `src/widgets/ChartWidget.tsx`

현재 기준:

- Todo 40
- 습관 30
- 예정 작업 15
- 집중 세션 15
- 해당 날짜에 데이터가 없는 항목은 제외하고 남은 항목의 가중치로 재계산합니다.
- 집중 세션은 현재 날짜만 계산합니다.

추후 검토 포인트:

- 사용자가 "목표와 Todo 기준이 겹치지 않게" 조심하고 싶다고 말했습니다.
- 목표는 생산성 점수에 직접 합산하지 않는 방향으로 정리했습니다.
- 추후 전체 가이드라인 페이지를 만들자는 이야기가 있었습니다.

### Todo

- 위젯: `src/widgets/TodoWidget.tsx`
- 페이지: `src/pages/TodoPage.tsx`
- 상태: `todos`, `todoHistory`, `todoHistoryTrash`, `todoHistoryDeletedDates`

현재 반영:

- Todo 위젯과 Todo 페이지는 같은 상태를 사용합니다.
- 진행률은 Todo 페이지와 생산성 추이에만 주요하게 사용하고, 위젯은 숫자 중심으로 간소화했습니다.
- 날짜별 기록 저장, 보정, 누락 항목 추가, 잘못된 항목 제거, 휴지통/복원/영구 삭제가 구현되어 있습니다.

### 일일 루틴

- 위젯: `src/widgets/HabitWidget.tsx`
- 페이지: `src/pages/HabitTracker.tsx`
- 상태: `habits`, `habitHistory`, `habitSavedAt`

현재 반영:

- 기본 루틴 최초 생성
- 사용자 루틴 추가/수정/삭제
- 반복 요일 설정
- 날짜별 기록
- 최근 1년 활동 그래프
- 위젯에서 오늘 기록 저장과 자동 반영

### 빠른 메모

- 위젯: `src/widgets/MemoWidget.tsx`
- 페이지: `src/pages/Notes.tsx`
- 상태: `quickMemos`, `notes`

현재 반영:

- 빠른 메모는 짧은 임시 메모 수집함 성격입니다.
- Todo 또는 정식 노트로 전환할 수 있습니다.
- 날짜별로 보관/처리 이력을 유지합니다.

### 집중 타이머와 카운터

- 타이머: `src/widgets/PomodoroWidget.tsx`
- 카운터: `src/widgets/CounterWidget.tsx`
- 카운터 유틸: `src/utils/counters.ts`

현재 반영:

- 집중 타이머 완료 시 집중 세션 카운터 자동 기록
- 카운터는 사용자 정의 숫자 추적기로 확장
- 운동 횟수 등 일/주/전체 단위 카운터 사용 가능
- 작은 크기에서도 타이머 숫자와 버튼이 보이도록 조정한 이력이 있습니다.

### 저널 알림 위젯

- 파일: `src/widgets/JournalWidget.tsx`
- n8n 웹훅: `https://dbp-jack.app.n8n.cloud/webhook/journal-widget`

현재 반영:

- 웹훅 URL은 코드에 고정되어 있습니다.
- 설정 UI는 제거했습니다.
- title/link만 있는 RSS 형태 응답도 처리합니다.
- 캐러셀 형태로 표시합니다.
- 이미지가 있으면 카드 전체를 채우고, 아래 중복 텍스트를 제거했습니다.
- 제목 라벨은 숨겼고 새로고침 버튼만 오버레이/액션으로 제공합니다.
- GitHub Pages 자동 배포 설정은 되돌렸습니다.

## 모바일/반응형 상태

완료된 이력:

- 모바일 대시보드는 데스크톱 그리드 대신 세로 카드형 위젯 구조로 렌더링합니다.
- 편집 화면에서 iPad/터치 환경에서도 위젯 추가 패널을 버튼으로 열고 닫을 수 있도록 보완했습니다.
- 편집 화면 삭제 버튼은 터치 이벤트 전파를 막는 처리가 있습니다.
- 각 하위 페이지 상단에 홈으로 바로 돌아가는 버튼이 있습니다.

남은 검토:

- SC-10 모바일 반응형은 정식 검토 전입니다.
- 모바일/태블릿에서 각 페이지 입력 폼, 카드 폭, 하단 탭, 사이드바 숨김 구조를 실제 기기 기준으로 다시 봐야 합니다.
- 사용자가 모바일 구조가 깨진 화면을 보여준 적이 있어, 배포본 기준 모바일 대시보드 확인은 중요합니다.

## 문서 상태와 정리 필요

문서:

- `REQUIREMENTS.md`
- `SCENARIOS.md`
- `PROGRESS.md`

현재 불일치:

- `PROGRESS.md`에는 SC-08이 "페이지 10개"로 표기되어 있습니다.
- `SCENARIOS.md`에는 SC-08이 "페이지 11개 구현"으로 되어 있습니다.
- 실제 `src/App.tsx` 기준 페이지는 14개 page id가 있습니다.

다음 작업 때 할 일:

- SC-08 검토 범위를 실제 페이지 목록 기준으로 다시 정리합니다.
- 이미 검토가 끝난 페이지와 아직 정식 검토 전인 페이지를 표로 분리하면 좋습니다.
- 요구사항 번호 중 `R-040`이 데이터 저장과 UX/테마 쪽에서 중복되어 있습니다. 필요하면 번호 정리도 해야 합니다.

## 다음 작업 제안 순서

사용자와 의논하며 하나씩 진행하세요.

1. 현재 배포본/로컬 상태 확인
2. 문서 불일치 정리: SC-08 페이지 수와 페이지 목록 업데이트
3. SC-08 페이지 검토 시작
4. 우선 검토 후보: 신청 관리, Todo, 습관, 목표, 작업 관리, 노트/저널
5. 각 페이지별로 데스크톱 → 태블릿 → 모바일 순서로 비율/입력/저장/연동 확인
6. 페이지 검토가 끝나면 SC-09 테마 최종 정리
7. 마지막으로 SC-10 모바일 반응형 전체 QA

## 다음 채팅 시작 문장 예시

```text
/Users/minsujeong/planner/NEXT_CHAT_HANDOFF.md 파일 읽고 현재 플래너 작업 이어서 진행해줘.
AGENTS.md 작업 방식도 같이 지켜줘. 일단 현재 상태 파악 후 SC-08 페이지 검토부터 하나씩 의논하면서 시작하자.
```

## 작업 시 주의

- 사용자와 상의 없이 여러 작업을 한 번에 진행하지 마세요.
- 사용자가 "커밋푸쉬"라고 말하기 전에는 보통 구현 후 확인을 먼저 받는 흐름이 좋습니다.
- 단, 사용자가 "작업하고 바로 커밋푸쉬"라고 명시한 경우는 구현, 검증, 커밋, 푸시까지 진행합니다.
- `.claude/settings.local.json`은 staging하지 마세요.
- Firebase API 키나 기상청 키 같은 값은 노출/커밋에 주의하세요.
- React Grid Layout은 `1.4.4` 고정입니다. v2로 올리지 마세요.
- `onLayoutChange` 저장 방식은 쓰지 말고, 드래그/리사이즈 완료 시점 저장 흐름을 유지하세요.
- 한국 날짜 계산은 UTC 밀림을 피해야 합니다. `src/utils/date.ts`의 로컬 날짜 유틸을 우선 사용하세요.
